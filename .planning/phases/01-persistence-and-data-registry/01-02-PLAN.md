---
phase: 01-persistence-and-data-registry
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/radar-data-sources.ts
  - lib/radar-persistence.ts
  - lib/radar-data.ts
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - O dataset embutido atual aparece como primeira origem versionada do sistema.
    - Existe um registro claro de origem ativa consumivel pelo dashboard.
  artifacts:
    - lib/radar-data-sources.ts
  key_links:
    - O registry de data sources usa os contratos persistidos definidos no Plano 01.
---

<objective>
Introduzir o registry de origens de dados versionadas e migrar a base embutida atual para esse modelo.

Purpose: desbloquear importacao e source switching sem reescrever o dashboard depois.
Output: registry de data sources, bootstrap da origem inicial e selectors de origem ativa.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-and-data-registry/01-CONTEXT.md
@.planning/phases/01-persistence-and-data-registry/01-RESEARCH.md
@.planning/phases/01-persistence-and-data-registry/01-01-PLAN.md
@lib/radar-data-source.ts
@lib/radar-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create versioned data source registry</name>
  <files>lib/radar-data-sources.ts, lib/radar-persistence.ts</files>
  <read_first>lib/radar-data-source.ts, lib/radar-persistence.ts</read_first>
  <action>Criar funcoes para bootstrapar a origem inicial a partir de `RAW_RADAR_DATA`, atribuindo `id`, `label`, `version`, `createdAt`, `recordCount`, `kind` e flag de origem ativa. A leitura e escrita devem reaproveitar os envelopes do Plano 01.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - existe um helper que retorna a origem ativa
    - a origem inicial embutida e registrada automaticamente quando nao houver persistence previa
  </acceptance_criteria>
  <done>O sistema conhece explicitamente suas origens de dados, mesmo sem importacao ainda.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor radar data selectors around active source</name>
  <files>lib/radar-data.ts, lib/radar-data-sources.ts</files>
  <read_first>lib/radar-data.ts, lib/radar-data-sources.ts</read_first>
  <action>Desacoplar os selectors do dashboard de `RADAR_DATA` singleton. Introduza selectors para obter dataset bruto da origem ativa e garanta compatibilidade com enriquecimento, filtro de periodo, KPI e cluster.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - o dominio tem um caminho claro para consumir a origem ativa
    - `RADAR_DATA` deixa de ser o unico contrato implicito do app
  </acceptance_criteria>
  <done>O pipeline de dados aceita origem ativa como entrada central.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc --noEmit`
- [ ] a origem embutida atual esta registrada como dataset versionado inicial
- [ ] selectors do dominio aceitam origem ativa sem quebrar enriquecimento
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Registry inicial de data sources pronto para a integracao do dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-and-data-registry/01-02-SUMMARY.md`
</output>
