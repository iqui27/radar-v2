---
phase: 01-persistence-and-data-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/radar-schemas.ts
  - lib/radar-persistence.ts
  - lib/radar-data.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
user_setup: []
must_haves:
  truths:
    - Configuracoes do score so podem ser persistidas quando os invariantes estiverem validos.
    - Snapshots de configuracao possuem metadata consistente para restauracao e comparacao futura.
  artifacts:
    - lib/radar-schemas.ts
    - lib/radar-persistence.ts
  key_links:
    - ConfigPanel e Dashboard passam a usar helpers tipados de persistence em vez de salvar estado cru.
---

<objective>
Criar a fundacao tipada de persistencia para configuracao e historicos do RADAR.

Purpose: estabelecer contratos estaveis antes de ligar UI e data sources.
Output: schemas zod, helpers de storage e funcoes de snapshot reutilizaveis.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-and-data-registry/01-CONTEXT.md
@.planning/phases/01-persistence-and-data-registry/01-RESEARCH.md
@lib/radar-data.ts
@components/dashboard/config-panel.tsx
@components/dashboard/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define persisted schemas and envelopes</name>
  <files>lib/radar-schemas.ts, lib/radar-persistence.ts</files>
  <read_first>lib/radar-data.ts, .planning/phases/01-persistence-and-data-registry/01-CONTEXT.md</read_first>
  <action>Criar os tipos e schemas para `RadarConfigSnapshot`, `SearchHistoryEntry`, `RadarDataSourceRecord` e `RadarPersistenceState`. Use `zod` para validar order constraints de thresholds e bands, e adote envelopes com `id`, `createdAt`, `version`, `label` e metadata minima.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `lib/radar-schemas.ts` exporta schemas nomeados para config snapshots e data sources
    - `lib/radar-persistence.ts` possui funcoes de leitura/escrita seguras com fallback
  </acceptance_criteria>
  <done>Persistence e schemas existem, compilam e cobrem os modelos da fase.</done>
</task>

<task type="auto">
  <name>Task 2: Add config validation helpers to domain layer</name>
  <files>lib/radar-data.ts</files>
  <read_first>lib/radar-schemas.ts, lib/radar-persistence.ts</read_first>
  <action>Extrair helpers reutilizaveis para validar invariantes de configuracao e normalizar dados restaurados antes de alimentar score, expectedCTR e filtros. Nao deixe a validacao enterrada apenas no componente.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `lib/radar-data.ts` expone helpers para sanitizar/validar configuracao
    - componentes futuros podem consumir o mesmo contrato sem duplicacao
  </acceptance_criteria>
  <done>O dominio aceita apenas configuracoes validas ou cai em fallback seguro.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc --noEmit`
- [ ] persistence contracts existem e sao importaveis
- [ ] invariantes de configuracao estao centralizados fora do JSX
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contracts de persistence e validacao prontos para as proximas integracoes
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-and-data-registry/01-01-SUMMARY.md`
</output>
