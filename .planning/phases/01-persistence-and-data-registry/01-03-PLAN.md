---
phase: 01-persistence-and-data-registry
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - hooks/use-radar-dashboard.ts
  - components/dashboard/index.tsx
  - components/dashboard/config-panel.tsx
  - components/dashboard/header.tsx
  - components/dashboard/search-panel.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - DATA-02
user_setup: []
must_haves:
  truths:
    - O dashboard carrega config persistida e origem ativa no bootstrap.
    - Salvar configuracao gera snapshot persistido sem quebrar a leitura atual.
    - A orquestracao central do dashboard passa a depender do novo estado local-first.
  artifacts:
    - hooks/use-radar-dashboard.ts
    - components/dashboard/index.tsx
  key_links:
    - ConfigPanel dispara save/reset usando o store persistido
    - Dashboard usa a origem ativa versionada para calcular `enrichedData`
---

<objective>
Integrar a fundacao persistida ao dashboard existente sem regressao de UX.

Purpose: fazer a nova infraestrutura realmente governar config e origem de dados da app.
Output: hook container do dashboard, bootstrap persistido e save snapshot funcional.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-and-data-registry/01-CONTEXT.md
@.planning/phases/01-persistence-and-data-registry/01-RESEARCH.md
@.planning/phases/01-persistence-and-data-registry/01-01-PLAN.md
@.planning/phases/01-persistence-and-data-registry/01-02-PLAN.md
@components/dashboard/index.tsx
@components/dashboard/config-panel.tsx
@components/dashboard/header.tsx
@components/dashboard/search-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize dashboard state in a local-first hook</name>
  <files>hooks/use-radar-dashboard.ts, components/dashboard/index.tsx</files>
  <read_first>components/dashboard/index.tsx, lib/radar-persistence.ts, lib/radar-data-sources.ts</read_first>
  <action>Criar um hook de orquestracao que concentre config atual, snapshots de config, origem ativa, periodo e termo selecionado. O `Dashboard` deve migrar para esse hook, reduzindo estado solto e preparando fases 2 e 3.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `components/dashboard/index.tsx` deixa de ser a unica fonte de verdade ad hoc para config e data source
    - o hook expone handlers claros para save, reset e troca de origem ativa
  </acceptance_criteria>
  <done>O dashboard tem um state container reutilizavel para a fase atual e as proximas.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ConfigPanel save flow to persisted snapshots</name>
  <files>components/dashboard/config-panel.tsx, components/dashboard/index.tsx, hooks/use-radar-dashboard.ts</files>
  <read_first>components/dashboard/config-panel.tsx, hooks/use-radar-dashboard.ts</read_first>
  <action>Trocar o save/reset atual para usar a camada persistida. Ao salvar, gerar snapshot restauravel; ao resetar, voltar para os defaults definidos. Nao entregar ainda a timeline completa da fase 2, mas deixe os dados disponiveis para ela.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - salvar deixa de ser apenas `setIsDirty(false)`
    - snapshots persistidos existem mesmo que a UI completa de historico venha depois
  </acceptance_criteria>
  <done>O fluxo de configuracao passa a ter efeito real e auditavel.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap dashboard calculations from active data source</name>
  <files>components/dashboard/index.tsx, components/dashboard/header.tsx, components/dashboard/search-panel.tsx</files>
  <read_first>components/dashboard/index.tsx, lib/radar-data.ts, lib/radar-data-sources.ts</read_first>
  <action>Conectar o shell do dashboard para calcular `filteredData`, `enrichedData`, KPIs e selecao de termo a partir da origem ativa do registry. Se necessario, adicione um placeholder discreto para a origem atual, mas sem tentar concluir a UX final do dropdown nesta fase.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - a origem ativa governa o conjunto de dados usado pelo dashboard
    - build e typecheck continuam verdes
  </acceptance_criteria>
  <done>A infraestrutura nova esta viva no produto, nao apenas em helpers isolados.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc --noEmit`
- [ ] `pnpm build`
- [ ] salvar configuracao deixa snapshot persistido pronto para restore futuro
- [ ] dashboard carrega dados a partir da origem ativa registrada
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dashboard operando sobre persistence local-first e source registry
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-and-data-registry/01-03-SUMMARY.md`
</output>
