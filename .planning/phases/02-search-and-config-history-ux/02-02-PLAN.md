---
phase: 02-search-and-config-history-ux
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/radar-history.ts
  - hooks/use-radar-dashboard.ts
  - components/dashboard/config-panel.tsx
autonomous: true
requirements:
  - CONF-03
user_setup: []
must_haves:
  truths:
    - O painel de Configuracao exibe snapshots salvos em ordem cronologica.
    - O usuario consegue restaurar um snapshot anterior com efeito real no dashboard.
  artifacts:
    - components/dashboard/config-panel.tsx
  key_links:
    - ConfigPanel usa o hook central para restaurar snapshots persistidos
---

<objective>
Transformar snapshots persistidos em historico restauravel de configuracao.

Purpose: recuperar a confianca operacional do dashboard antigo, mas com UX mais limpa.
Output: timeline/lista de snapshots e fluxo de restore funcional.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-config-history-ux/02-CONTEXT.md
@.planning/phases/02-search-and-config-history-ux/02-RESEARCH.md
@hooks/use-radar-dashboard.ts
@components/dashboard/config-panel.tsx
@lib/radar-persistence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose snapshot restore flow from the dashboard state container</name>
  <files>hooks/use-radar-dashboard.ts, lib/radar-history.ts</files>
  <read_first>hooks/use-radar-dashboard.ts, lib/radar-persistence.ts, lib/radar-history.ts</read_first>
  <action>Adicionar selectors e handlers para listar snapshots salvos, identificar snapshot atual/relevante e restaurar configuracao persistida de forma segura. Restore deve atualizar config ativa e refletir no restante do dashboard.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - o hook expone snapshots ordenados e um handler de restore
    - restore passa pelo state container central, nao por state local do componente
  </acceptance_criteria>
  <done>Existe um fluxo seguro e reutilizavel de restore.</done>
</task>

<task type="auto">
  <name>Task 2: Render config history and restore UI in ConfigPanel</name>
  <files>components/dashboard/config-panel.tsx</files>
  <read_first>components/dashboard/config-panel.tsx, hooks/use-radar-dashboard.ts</read_first>
  <action>Adicionar uma secao de historico de configuracoes no painel esquerdo ou na area mais apropriada, com timestamp, contexto minimo e botao de restaurar. A interface deve lembrar o dashboard antigo, mas com apresentacao mais limpa e discreta.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - ConfigPanel mostra snapshots salvos com CTA de restore
    - restaurar um snapshot atualiza os valores exibidos no painel
  </acceptance_criteria>
  <done>Historico de configuracao fica visivel e restauravel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc --noEmit`
- [ ] `pnpm build`
- [ ] restore de snapshot reflete na configuracao atual e no preview
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Historico de configuracao ficou visivel e operacional
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-config-history-ux/02-02-SUMMARY.md`
</output>
