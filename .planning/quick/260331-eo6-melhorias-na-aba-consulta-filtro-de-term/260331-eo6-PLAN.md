---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/search-panel.tsx
  - components/dashboard/term-cluster.tsx
  - components/dashboard/scatter-chart.tsx
  - lib/radar-data.ts
autonomous: true
requirements:
  - QT-01  # Filtro de marca
  - QT-02  # Clusterização semântica
  - QT-03  # Resumo do cluster
  - QT-04  # Box de termos do cluster
  - QT-05  # Impressões como primeira métrica
  - QT-06  # Scatter clusterizado
  - QT-07  # Modal com métricas
  - QT-08  # Clusters relacionados
---

<objective>
Implementar 8 melhorias na aba consulta do radar analytics dashboard.

Purpose: Melhorar a análise de termos com filtragem de marca, clustering semântico, visualização de cluster e scatter plot clusterizado.
Output: Componentes atualizados em search-panel.tsx, term-cluster.tsx, scatter-chart.tsx e lib/radar-data.ts
</objective>

<context>
@components/dashboard/search-panel.tsx
@components/dashboard/term-cluster.tsx
@components/dashboard/scatter-chart.tsx
@lib/radar-data.ts

**Estado atual:**
- Clustering: similaridade word-based simples (substring match)
- Scatter colors: por action (Evitar/Avaliar/Testar/Investir)
- Métricas no termo analisado: Position → CTR → Cliques → Impressoes (4a posição)
- Termos relacionados: pílulas clicáveis + Dialog para ver mais
- Filtro de marca: NÃO existe

**Decisões:**
- Clustering semântico: implementar usando similaridade semântica (n-gram overlap + edit distance para robustez)
- Filtro de marca: dropdown com checkboxes
- Scatter clusterizado: usar clusterId para cores
- Cluster summary e box de termos: adicionar na UI existente
- Modal para métricas: dialog com métricas detalhadas
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand filter + Reorder metrics</name>
  <files>components/dashboard/search-panel.tsx, lib/radar-data.ts</files>
  <action>
  **1a. Adicionar filtro de marca em search-panel.tsx:**
  - Adicionar estado `brandFilter: { includeBB: boolean, excludeBB: boolean }` em SearchPanel
  - Criar componente de filtro com dropdown + checkboxes (incluir BB/remover BB)
  - Dropdown aparece próximo ao contador de termos disponíveis
  - Aplicar filtro nos dados antes de passar para TermCluster
  
  **1b. Reordenar métricas em TermAnalysisCard:**
  - Mudar ordem do array `stats` para: Impressoes (primeira) → Posicao → CTR → Cliques
  - Atualizar ícone para Eye (já importado)
  - Manter lógica de agregação/individual intacta
  </action>
  <verify>npm run build 2>&1 | head -50 && echo "BUILD OK"</verify>
  <done>Dropdown de filtro de marca visível e funcional; Impressões aparece como primeira métrica no card</done>
</task>

<task type="auto">
  <name>Task 2: Semantic clustering + Cluster summary + Terms box</name>
  <files>lib/radar-data.ts, components/dashboard/search-panel.tsx, components/dashboard/term-cluster.tsx</files>
  <action>
  **2a. Implementar clustering semântico em lib/radar-data.ts:**
  - Modificar `getRelatedTerms()` para usar similaridade semântica:
    - Usar n-gram overlap (bigrams) entre termos
    - Combinar com edit distance (Levenshtein) para variações de marca
    - Calcular `semanticSimilarity = ngramOverlap * 0.7 + editDistanceNorm * 0.3`
  - Adicionar campo `clusterId` nos dados enriquecidos
  - Adicionar função `getRelatedClusters()` que retorna clusters similares
  
  **2b. Adicionar cluster summary em TermAnalysisCard:**
  - No header do card, após o badge "X relacionados", adicionar seção:
    ```
    Cluster #X | Y termos | Score médio: Z.ZZ | CTR: W.WW%
    ```
  - Calcular resumo a partir de `calculateClusterMetrics()`
  
  **2c. Adicionar box de termos do cluster:**
  - Criar seção collapsible "Termos do cluster" abaixo das pílulas
  - Mostrar grid de badges com todos os termos do cluster
  - Destacar o termo selecionado com borda colorida
  - Permitir clicar para selecionar outro termo
  </action>
  <verify>npm run build 2>&1 | head -50 && echo "BUILD OK"</verify>
  <done>Clusterização é semântica; card mostra resumo do cluster e box com todos os termos</done>
</task>

<task type="auto">
  <name>Task 3: Scatter by cluster + Metrics modal + Related clusters</name>
  <files>components/dashboard/scatter-chart.tsx, components/dashboard/term-cluster.tsx, lib/radar-data.ts</files>
  <action>
  **3a. Scatter chart clusterizado:**
  - Em scatter-chart.tsx, modificar para colorir por clusterId em vez de score/action
  - Gerar paleta de cores para clusters (até 12 cores distintas)
  - No legend, mostrar "Cluster 1", "Cluster 2", etc. em vez de action labels
  - Manter tooltip mostrando cluster ID e métricas existentes
  
  **3b. Modal de métricas para termos relacionados:**
  - No Dialog de "Mais termos relacionados" (search-panel.tsx), adicionar métricas:
    - Para cada termo: Position, CTR, Clicks, Impressoes em grid
    - Usar `font-mono` para números
    - Badge colorido com score
  - Permitir click no termo fecha dialog e seleciona termo
  
  **3c. Clusters relacionados no lugar de termos relacionados:**
  - Em TermAnalysisCard, substituir seção "X relacionados" por "Clusters relacionados"
  - Mostrar badges clicáveis com nome do cluster e score médio
  - Ao clicar, expandir para mostrar termos daquele cluster
  - Manter dialog "Mais termos" para clusters com muitos termos
  </action>
  <verify>npm run build 2>&1 | head -50 && echo "BUILD OK"</verify>
  <done>Scatter usa cores por cluster; modal mostra métricas detalhadas; UI usa clusters relacionados</done>
</task>

</tasks>

<verification>
- Build completa sem erros
- Filtro de marca funcional com checkbox
- Métricas mostram Impressões primeiro
- Clusterização usa similaridade semântica (verificar via console.log ou inspeção)
- Scatter plot colore por cluster
- Modal de termos relacionados mostra métricas
</verification>

<success_criteria>
1. Filtro de marca (dropdown com incluir/excluir BB) está visível e filtra termos
2. Impressões é a primeira métrica no card do termo analisado
3. Clustering é semântico (n-gram + edit distance)
4. TermAnalysisCard mostra resumo do cluster e box com termos
5. Scatter plot usa cores por cluster
6. Modal de termos relacionados mostra métricas (Position, CTR, Clicks, Impressoes)
7. UI mostra clusters relacionados ao invés de apenas termos relacionados
</success_criteria>

<output>
After completion, create `.planning/quick/260331-eo6-melhorias-na-aba-consulta-filtro-de-term/260331-eo6-SUMMARY.md`
</output>
